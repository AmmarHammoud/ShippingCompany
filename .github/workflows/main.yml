on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    # - name: 📂 Sync files
    #   uses: SamKirkland/FTP-Deploy-Action@v4.3.6
    #   with:
    #     server: win8117.site4now.net
    #     username: fastship-001
    #     password: ${{secrets.PASSWORD}}
    # Upload everything except public/ to the root (outside wwwroot)

    - name: 📂 Upload project to SmarterASP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: win8117.site4now.net
        username: fastship-001
        password: ${{ secrets.PASSWORD }}
        local-dir: ./
        remote-path: www/shipping-company/
        git-ftp-args: "--all --exclude public/* --exclude vendor/* --exclude .env --exclude .git* --exclude node_modules/*"
        # git-ftp-args: "--exclude public/* --exclude .git --exclude node_modules"
        # dangerous-clean-slate: true
